using System.Collections;
using System.Collections.Generic;
using UnityEngine;
using UnityEngine.UI;

public class Health : MonoBehaviour
{
    public int maxHealth;
    public int curHealth;
    
    private void Awake() {
        
    }

    
    //create a health bar at the transform position on the screen
    //and then move it up
    //but that mean anythin that isnt the player needs it position updated
}
